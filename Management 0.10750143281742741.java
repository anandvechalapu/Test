·       User should be able to see and access the sub screens of management functionality.
·       User should be able to create, edit, view the sub screens.

Solution:
This application can be developed using Spring Boot. The application will contain a controller, service and repository classes for the user story.

Controller:
The controller will handle the user requests and calls the appropriate services. It will validate the user input and return the appropriate response.

Service:
The service layer will contain the business logic for the user story. It will handle the requests from the controller and call the repository layer for database operations.

Repository:
The repository layer will handle the database operations. It will contain methods to create, edit, view the sub screens of the management functionality.